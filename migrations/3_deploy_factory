const HumanStandardTokenFactory =
artifacts.require(`./BadgeForceTokenFactory.sol`)

module.exports = (deployer) => {
deployer.deploy(HumanStandardTokenFactory)
}

/*
BadgeForceTokenFactory.deployed().then(function(c){c.createBadgeForceToken(10, "BadgeForceToken", 3, "BFC").then(console.log)})

BadgeForceToken.deployed().then(function(c){c.name().then(console.log)})
*/